<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lyread="clr-namespace:Lyread;assembly=Lyread"
             x:Class="Lyread.TocPage"
             Title="Contents">

    <ContentPage.Icon>
        <OnPlatform x:TypeArguments="FileImageSource" Android="@drawable/ic_toc_white_24dp" WinPhone="Icons\toc.png"/>
    </ContentPage.Icon>

    <ContentPage.BindingContext>
        <lyread:TocViewModel x:Name="TocViewModel"/>
    </ContentPage.BindingContext>

    <CollectionView x:Name="TocView"
                    ItemsSource="{Binding TocItems}"
                    SelectionMode="Single"
                    SelectionChangedCommand="{Binding OpenTocItemCommand}"
                    SelectionChangedCommandParameter="{Binding SelectedItem, Source={x:Reference TocView}}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Label Text="{Binding Title}" />
                <!--<TextCell.ContextActions>
                        <MenuItem Text="Open" Command="{Binding OpenDocumentCommand, Source={x:Reference TocViewModel}}" CommandParameter="{Binding}"/>
                    </TextCell.ContextActions>-->
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>